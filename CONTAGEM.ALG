algoritmo "contagem"
var
  contador, num: inteiro
inicio
    contador <- 0
    Escreval ("Até qual número você deseja contar? ")
    leia(num)
    Enquanto (contador <= num) faca
        Escreval (contador)
        contador <- contador + 1
     FimEnquanto
    
fimalgoritmo