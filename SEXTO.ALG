algoritmo "sexto"
var
  angulo, seno: real
inicio
     Escreval("Digite o ângulo: ")
     Leia(angulo)
     seno<- Sen(GraupRad(angulo))
     Escreva("O seno de ", angulo, " é igual a: ", seno)
fimalgoritmo