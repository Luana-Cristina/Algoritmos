algoritmo "precoproduto"
var
   produto, imposto: real
inicio
     escreval("Digite o preço do seu produto: ")
     leia(produto)
      imposto<- (produto*60) / 100
      Escreval("O valor do imposto: ", imposto)
fimalgoritmo